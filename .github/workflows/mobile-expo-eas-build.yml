name: "Reusable: Expo EAS Build"

on:
  workflow_call:
    inputs:
      working-directory:
        required: false
        type: string
        default: "."
        description: "Expo project directory"
      system-name:
        required: true
        type: string
        description: "System name for artifacts/display"
      node-version:
        required: false
        type: number
        default: 20
        description: "Node.js version"
      platform:
        required: true
        type: string
        description: "EAS build platform (android|ios)"
      build-profile:
        required: false
        type: string
        default: "production"
        description: "EAS build profile name"
      upload-artifact:
        required: false
        type: boolean
        default: true
        description: "Download the EAS artifact and upload it to GitHub"
    outputs:
      build-id:
        description: "EAS build identifier"
        value: ${{ jobs.expo-eas-build.outputs.build_id }}
      build-details-url:
        description: "EAS build details URL"
        value: ${{ jobs.expo-eas-build.outputs.build_details_url }}
      artifact-url:
        description: "Artifact URL returned by EAS"
        value: ${{ jobs.expo-eas-build.outputs.artifact_url }}
    secrets:
      EXPO_TOKEN:
        required: false
      EXPO_PROJECT_ID:
        required: false
      EXPO_OWNER:
        required: false

permissions:
  contents: read

jobs:
  expo-eas-build:
    name: "Expo EAS Build (${{ inputs.platform }})"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    outputs:
      build_id: ${{ steps.parse.outputs.build_id }}
      build_details_url: ${{ steps.parse.outputs.build_details_url }}
      artifact_url: ${{ steps.parse.outputs.artifact_url }}
    defaults:
      run:
        working-directory: ./${{ inputs.working-directory }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Detect package manager
        id: detect-pm
        run: |
          if [ -f "yarn.lock" ]; then
            echo "pm=yarn" >> "$GITHUB_OUTPUT"
          elif [ -f "pnpm-lock.yaml" ]; then
            echo "pm=pnpm" >> "$GITHUB_OUTPUT"
          elif [ -f "package-lock.json" ] || [ -f "npm-shrinkwrap.json" ] || [ -f "package.json" ]; then
            echo "pm=npm" >> "$GITHUB_OUTPUT"
          else
            echo "❌ package.json is required for Expo EAS builds."
            exit 1
          fi

      - name: Setup Node.js ${{ inputs.node-version }} (npm)
        if: ${{ steps.detect-pm.outputs.pm == 'npm' }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: "npm"
          cache-dependency-path: |
            **/package-lock.json

      - name: Setup Node.js ${{ inputs.node-version }} (yarn)
        if: ${{ steps.detect-pm.outputs.pm == 'yarn' }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: "yarn"
          cache-dependency-path: |
            **/yarn.lock

      - name: Setup Node.js ${{ inputs.node-version }} (pnpm)
        if: ${{ steps.detect-pm.outputs.pm == 'pnpm' }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: "pnpm"
          cache-dependency-path: |
            **/pnpm-lock.yaml

      - name: Install Dependencies
        env:
          PACKAGE_MANAGER: ${{ steps.detect-pm.outputs.pm }}
        run: |
          if [ "${PACKAGE_MANAGER}" = "yarn" ]; then
            corepack enable
            yarn install --frozen-lockfile --ignore-scripts
          elif [ "${PACKAGE_MANAGER}" = "pnpm" ]; then
            corepack enable
            pnpm install --frozen-lockfile --ignore-scripts
          else
            npm ci --ignore-scripts
          fi

      - name: Validate Expo EAS configuration
        env:
          BUILD_PLATFORM: ${{ inputs.platform }}
          BUILD_PROFILE: ${{ inputs.build-profile }}
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_PROJECT_ID: ${{ secrets.EXPO_PROJECT_ID }}
          EXPO_OWNER: ${{ secrets.EXPO_OWNER }}
        run: |
          if [ -z "${EXPO_TOKEN}" ]; then
            echo "❌ EXPO_TOKEN secret is required for Expo EAS builds."
            exit 1
          fi

          for REQUIRED_FILE in package.json tsconfig.json eas.json; do
            if [ ! -f "${REQUIRED_FILE}" ]; then
              echo "❌ ${REQUIRED_FILE} is required for Expo EAS builds."
              exit 1
            fi
          done

          if [ ! -f "app.json" ] && [ ! -f "app.config.js" ] && [ ! -f "app.config.ts" ]; then
            echo "❌ Expo app config is required (app.json, app.config.js, or app.config.ts)."
            exit 1
          fi

          if [ -n "${EXPO_PROJECT_ID}" ] || [ -n "${EXPO_OWNER}" ]; then
            echo "ℹ️ Expo project identity will be supplied from CI secrets when possible."
          fi

          node <<'NODE'
          const fs = require('fs');

          const platform = process.env.BUILD_PLATFORM;
          const profileName = process.env.BUILD_PROFILE;
          const projectIdFromSecret = process.env.EXPO_PROJECT_ID || '';
          const ownerFromSecret = process.env.EXPO_OWNER || '';

          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const deps = {
            ...(pkg.dependencies || {}),
            ...(pkg.devDependencies || {}),
            ...(pkg.peerDependencies || {}),
          };

          if (!deps.expo) {
            throw new Error('Expo standard requires dependency "expo".');
          }

          if (!deps.typescript) {
            throw new Error('Expo standard requires dependency "typescript".');
          }

          const eas = JSON.parse(fs.readFileSync('eas.json', 'utf8'));
          const profile = eas.build && eas.build[profileName];
          if (!profile) {
            throw new Error(`eas.json is missing build profile "${profileName}".`);
          }

          const image = profile[platform] && profile[platform].image;
          if (!image) {
            throw new Error(`eas.json build profile "${profileName}" must define ${platform}.image.`);
          }

          const hasAppJson = fs.existsSync('app.json');
          const hasDynamicAppConfig = fs.existsSync('app.config.js') || fs.existsSync('app.config.ts');

          if (!projectIdFromSecret && !hasAppJson && hasDynamicAppConfig) {
            // Dynamic app config may still provide linkage; leave this to EAS.
            process.exit(0);
          }

          if (!projectIdFromSecret && hasAppJson) {
            const appJson = JSON.parse(fs.readFileSync('app.json', 'utf8'));
            const expo = appJson.expo || {};
            const projectId = (((expo.extra || {}).eas || {}).projectId) || '';
            if (!projectId && !hasDynamicAppConfig) {
              throw new Error('Expo app must define project linkage in app.json (expo.extra.eas.projectId) or provide EXPO_PROJECT_ID in CI secrets.');
            }
          }
          NODE

          # Resolve final Expo config (supports app.json and dynamic app.config.*)
          # and enforce platform identifiers required by EAS non-interactive builds.
          npx --yes expo config --json --type public > expo-public-config.json

          if [ "${BUILD_PLATFORM}" = "ios" ]; then
            if ! jq -e '((.expo // .).ios.bundleIdentifier // "") | type == "string" and length > 0' expo-public-config.json >/dev/null; then
              echo "❌ The ios.bundleIdentifier is required in Expo app config for non-interactive iOS EAS builds."
              echo "Set expo.ios.bundleIdentifier in app.json/app.config.(js|ts)."
              exit 1
            fi
          fi

          if [ "${BUILD_PLATFORM}" = "android" ]; then
            if ! jq -e '((.expo // .).android.package // "") | type == "string" and length > 0' expo-public-config.json >/dev/null; then
              echo "❌ The android.package is required in Expo app config for non-interactive Android EAS builds."
              echo "Set expo.android.package in app.json/app.config.(js|ts)."
              exit 1
            fi
          fi

      - name: Inject Expo project identity from CI
        env:
          EXPO_PROJECT_ID: ${{ secrets.EXPO_PROJECT_ID }}
          EXPO_OWNER: ${{ secrets.EXPO_OWNER }}
        run: |
          if [ -z "${EXPO_PROJECT_ID}" ] && [ -z "${EXPO_OWNER}" ]; then
            echo "ℹ️ No CI-provided Expo project identity to inject."
            exit 0
          fi

          if [ -f "app.json" ]; then
            cp app.json app.json.ci-backup
            jq \
              --arg projectId "${EXPO_PROJECT_ID}" \
              --arg owner "${EXPO_OWNER}" \
              '
              .expo = (.expo // {})
              | (if $owner != "" then .expo.owner = $owner else . end)
              | (if $projectId != ""
                 then .expo.extra = (.expo.extra // {})
                 | .expo.extra.eas = (.expo.extra.eas // {})
                 | .expo.extra.eas.projectId = $projectId
                 else .
                 end)
              ' app.json > app.json.ci-updated
            mv app.json.ci-updated app.json
            echo "✅ Injected Expo project identity into app.json for CI."
          else
            echo "ℹ️ Dynamic Expo app config detected. EXPO_PROJECT_ID and EXPO_OWNER will be exposed as environment variables for app.config.js/app.config.ts to consume."
          fi

      - name: Prepare Android native sources for RN compatibility
        if: ${{ inputs.platform == 'android' }}
        run: |
          if [ ! -d "android" ]; then
            echo "ℹ️ Android directory not found. Running Expo prebuild for android."
            CI=1 npx --yes expo prebuild --platform android --non-interactive
          fi

          mapfile -t MAIN_APP_FILES < <(find . -type f -path '*/android/app/src/main/java/*/MainApplication.java')

          if [ "${#MAIN_APP_FILES[@]}" -eq 0 ]; then
            echo "ℹ️ No MainApplication.java files found to normalize."
            exit 0
          fi

          for MAIN_APP in "${MAIN_APP_FILES[@]}"; do
            if grep -q 'DefaultNewArchitectureEntryPoint\.releaseLevel' "${MAIN_APP}"; then
              perl -0pi -e 's/DefaultNewArchitectureEntryPoint\.releaseLevel\s*=\s*([^;]+);/DefaultNewArchitectureEntryPoint.setReleaseLevel($1);/g' "${MAIN_APP}"
              echo "✅ Normalized releaseLevel API usage in ${MAIN_APP}"
            else
              echo "ℹ️ ${MAIN_APP} already uses compatible release level API."
            fi
          done

      - name: Trigger Expo EAS build
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_PROJECT_ID: ${{ secrets.EXPO_PROJECT_ID }}
          EXPO_OWNER: ${{ secrets.EXPO_OWNER }}
        run: |
          set -o pipefail

          if ! npx --yes eas-cli@latest build \
            --platform "${{ inputs.platform }}" \
            --profile "${{ inputs.build-profile }}" \
            --non-interactive \
            --wait \
            --json > eas-build-result.json 2> eas-build-error.log; then

            if [ "${{ inputs.platform }}" = "ios" ] && \
              grep -Eq 'Credentials are not set up|Distribution Certificate is not validated for non-interactive builds|Failed to set up credentials' eas-build-error.log; then
              echo "❌ iOS EAS non-interactive build failed because iOS credentials are not fully configured on Expo."
              echo ""
              echo "How to fix once (interactive setup):"
              echo "  1) Run locally: npx eas login"
              echo "  2) Run locally: npx eas credentials --platform ios"
              echo "  3) Create or upload Distribution Certificate + Provisioning Profile"
              echo "  4) Re-run CI"
              echo ""
              echo "Alternative: configure iOS credentials in Expo dashboard (Project -> Credentials)."
              echo ""
              echo "Tip: add expo.ios.infoPlist.ITSAppUsesNonExemptEncryption as true/false in app config"
              echo "to avoid App Store export compliance warnings during iOS builds."
            fi

            echo "--- EAS stderr (tail) ---"
            tail -n 80 eas-build-error.log || true
            exit 1
          fi

      - name: Parse EAS build result
        id: parse
        run: |
          BUILD_ID=$(jq -r 'if type=="array" then (.[0].id // "") else (.id // "") end' eas-build-result.json)
          BUILD_DETAILS_URL=$(jq -r 'if type=="array" then (.[0].webUrl // .[0].buildDetailsPageUrl // .[0].logsUrl // "") else (.webUrl // .buildDetailsPageUrl // .logsUrl // "") end' eas-build-result.json)
          ARTIFACT_URL=$(jq -r 'if type=="array" then (.[0].artifacts.buildUrl // .[0].artifacts.applicationArchiveUrl // .[0].artifacts.xcarchiveUrl // .[0].artifacts.url // "") else (.artifacts.buildUrl // .artifacts.applicationArchiveUrl // .artifacts.xcarchiveUrl // .artifacts.url // "") end' eas-build-result.json)

          echo "build_id=${BUILD_ID}" >> "$GITHUB_OUTPUT"
          echo "build_details_url=${BUILD_DETAILS_URL}" >> "$GITHUB_OUTPUT"
          echo "artifact_url=${ARTIFACT_URL}" >> "$GITHUB_OUTPUT"

          echo "✅ EAS build completed: ${BUILD_ID}"
          if [ -n "${BUILD_DETAILS_URL}" ]; then
            echo "ℹ️ Build details: ${BUILD_DETAILS_URL}"
          fi

      - name: Download Expo artifact
        if: ${{ inputs.upload-artifact && steps.parse.outputs.artifact_url != '' }}
        env:
          ARTIFACT_URL: ${{ steps.parse.outputs.artifact_url }}
          PLATFORM: ${{ inputs.platform }}
          SYSTEM_NAME: ${{ inputs.system-name }}
        run: |
          mkdir -p build-artifacts

          FILE_NAME=$(basename "${ARTIFACT_URL%%\?*}")
          if [ -z "${FILE_NAME}" ] || [ "${FILE_NAME}" = "/" ] || [[ "${FILE_NAME}" != *.* ]]; then
            if [ "${PLATFORM}" = "android" ]; then
              FILE_NAME="${SYSTEM_NAME}-android-build.bin"
            else
              FILE_NAME="${SYSTEM_NAME}-ios-build.bin"
            fi
          fi

          curl -fsSL "${ARTIFACT_URL}" -o "build-artifacts/${FILE_NAME}"

      - name: Upload Expo artifact
        if: ${{ inputs.upload-artifact && steps.parse.outputs.artifact_url != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.system-name }}-${{ inputs.platform }}-build
          path: ${{ inputs.working-directory }}/build-artifacts/*
          retention-days: 14
          if-no-files-found: error

      - name: Restore Expo config
        if: ${{ always() }}
        run: |
          if [ -f "app.json.ci-backup" ]; then
            mv app.json.ci-backup app.json
          fi